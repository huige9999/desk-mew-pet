{
  "name": "desk-mew-pet_demo",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "dependencies": {
    "@tauri-apps/api": "^2.0.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "tauri:build:portable": "tauri build --no-bundle"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.10.0",
    "typescript": "~5.9.3",
    "vite": "^7.3.1"
  }
}
